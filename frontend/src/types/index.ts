/**
 * Shared Types - Frontend
 * Re-export des types du backend pour garantir la synchronisation parfaite (Single Source of Truth)
 */

// On ré-exporte tout depuis le backend
export * from '@backend/shared/types';
